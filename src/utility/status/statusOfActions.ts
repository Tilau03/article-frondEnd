export type statusActionsTypes = "not loaded" | "loading" | "loaded" | "error";

export enum statusAction {
  "NO_LOADED" = "not loaded",
  "LOADING" = "loading",
  "LOADED" = "loaded",
  "ERROR" = "error",
}
